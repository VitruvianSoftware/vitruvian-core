name: Copybara Export (homelab)

# Exports vitruvian-core/homelab/ to VitruvianSoftware/homelab on every push under
# homelab/**. Thin caller — logic lives in _copybara-export.yaml.
on:
    push:
        branches: [main]
        paths: ["homelab/**"]
    workflow_dispatch:
        inputs:
            copybara_options:
                description: "Copybara CLI options. Use '--force --ignore-noop' ONCE to seed the baseline; normal runs use --ignore-noop."
                default: "--ignore-noop"
                required: false

permissions:
    contents: read

concurrency:
    group: copybara-export-homelab
    cancel-in-progress: false

jobs:
    export:
        uses: ./.github/workflows/_copybara-export.yaml
        with:
            component: homelab
            standalone_only: ".github/workflows/sync-to-monorepo.yaml"
            copybara_options: ${{ github.event.inputs.copybara_options || '--ignore-noop' }}
        secrets:
            sync_ssh_key: ${{ secrets.HOMELAB_SYNC_SSH_KEY }}
