# Implementation Plan: [Feature/Refactor Title]

**Date:** [YYYY-MM-DD]
**Idea/Issue Reference:** [e.g., Idea 55]

---

## 1. Deep Research & Context

* **Objective:** [What is the goal of this implementation?]
* **Sibling Implementations:** [List at least one existing pattern/file studied to ensure architectural consistency. e.g., "Studied `cmd/db_spawn.go` to understand how context and state are handled."]
* **Gap Analysis:** [Compare this plan against the existing codebase. Call out any inconsistencies or latent bugs found during research that need to be addressed or avoided.]

## 2. Design Decisions

### Design Principles Alignment

*Every implementation must align with our codified principles (see `docs/analysis/PRODUCT_ANALYSIS.md`). Check the ones that apply and explain how:*

* [ ] **One CLI, everything** — Does this belong in devx or is it better as an external tool?
* [ ] **Convention over configuration** — Are sensible defaults provided? Is zero-config the happy path?
* [ ] **Transparency** — Does the command show explicit impact summaries before destructive actions?
* [ ] **Idempotency** — Is this operation safely repeatable without side effects?
* [ ] **AI-native** — Does it support `--json` structured output, deterministic exit codes, and `--dry-run`?
* [ ] **CLI + YAML parity** — Can this be configured both via CLI flags and `devx.yaml`?
* [ ] **Optimized Inner Loop** — Does this reduce the "code-to-feedback" cycle time?
* [ ] **Client-Driven Architecture** — No permanent cluster-side controllers; ephemeral agents only?
* [ ] **Absolute Portability** — Does it work identically across macOS/Linux, Intel/ARM?

### Non-Obvious Choices
* [Explicitly call out non-obvious choices.]
* [Explain why alternative approaches were rejected.]

## 3. Proposed Changes

*Group files by component (e.g., package, feature area, CLI layer) and order logically (dependencies first).*

### [Component Name / Package Name]

* **`[MODIFY]` [file_path.go]:** 
  * [Detailed description of specific, concrete changes]
  * [List any struct signature or function parameter changes]
* **`[NEW]` [file_path.go]:**
  * [Description of the new file's purpose and contents]
* **`[DELETE]` [file_path.go]:**
  * [Reason for deletion]

### Architecture & Runtime Compliance Checklist

* [ ] **Provider Abstraction:** Does this interact with containers? If so, it MUST use `provider.ContainerRuntime` and avoid `exec.LookPath`.
* [ ] **Config Cascade:** Does it respect the discovery priority (`Upward Walk` → `include` → `~/.devx/config.yaml` → `DevHostname`)?
* [ ] **Global Flags:** Explicitly state how `--dry-run`, `-y`, `--json`, and `--provider` are handled.
* [ ] **Error Handling Strategy:** Define fail-fast vs. interactive recovery and error message quality.
* [ ] **Environment & Context:** Specify WHERE commands execute (host vs. container vs. VM) and what assumptions are made.
* [ ] **Pre-push Hook / `devx audit`:** Does this change exit codes, container execution paths, or scanning behavior? If so, assess impact on the pre-push guardrail (`hook.go`).

## 4. Configuration & Schema Changes

*If the feature introduces new YAML keys, environment variables, or CLI flags, specify the exact schema.*

* [ ] **`devx.yaml` keys:** [List any new top-level or nested YAML keys. Update `devx.yaml.example` with documented examples.]
* [ ] **`cluster.yaml` keys:** [List any new cluster config keys. Update `cluster.yaml.example` with examples.]
* [ ] **Environment variables:** [List any new env vars read by the feature, including naming convention (e.g., `DEVX_*`).]
* [ ] **CLI flags:** [List new flags added to commands, including defaults and validation.]

## 5. Documentation Ecosystem (Mandatory)

*Documentation updates are never optional. The following must be updated AFTER successful verification.*

### Official Docs & CLI
* [ ] **Official Docs (Vitepress):** Update `docs/guide/*.md` and wire new pages into `docs/.vitepress/config.mjs` sidebar.
* [ ] **CLI Help Text:** Update `Use`, `Short`, `Long`, and `Example` fields in Cobra `cmd/*.go` definitions. Ensure proper `GroupID` assignment.
* [ ] **Environment Health (`devx doctor`):** Update `cmd/doctor.go` if the feature introduces new system dependencies or core capabilities.
* [ ] **Main README:** Update high-level feature summaries, CLI reference table, and add usage examples if applicable.
* [ ] **Feature Trackers:** Migrate the item from `IDEAS.md` to `FEATURES.md` with full write-up.
* [ ] **CHANGELOG:** Will be auto-generated by release-please from conventional commit messages — verify commit messages are correct.

### Agent Skills Templates (Mandatory Audit)

> [!CAUTION]
> These are the AI agent SOPs. If command signatures, flags, structured output (JSON), exit codes, or error messages change, ALL of these must be updated to prevent agents from hallucinating stale commands.

* [ ] `.agents/skills/devx/SKILL.md`
* [ ] `.agents/skills/platform-engineer/SKILL.md`
* [ ] `.github/skills/devx/SKILL.md`
* [ ] `.github/skills/platform-engineer/SKILL.md`

### Example Configs & Templates
* [ ] **`devx.yaml.example`:** Add commented examples of any new YAML configuration keys.
* [ ] **`cluster.yaml.example`:** Update if cluster-related changes are made.
* [ ] **Scaffold templates (`internal/scaffold/templates/`):** Update generated `devx.yaml` in project templates if schema changes.
* [ ] **`CONTRIBUTING.md`:** Update the project structure diagram and "Adding a New Command" section if new packages or commands are added.

## 6. Verification Plan

*Verification must test edge cases, not just the happy path.*

### Automated Tests
* [ ] Add/update unit tests: `go test ./... -count=1`
* [ ] Run Linters: `go vet ./...` / `staticcheck ./...`
* [ ] License headers: `mage licensecheck`
* [ ] Build verification: `go build ./...`
* [ ] Cross-platform CI: All 7 GitHub Actions checks must pass (Lint, Test, Validate Butane, Build ×4 platforms)

### Manual / Edge Case Verification
* [ ] **Edge Case 1:** [e.g., What if the container runtime isn't running?] → *Expected behavior: ...*
* [ ] **Edge Case 2:** [e.g., What if config file is missing?] → *Expected behavior: ...*
* [ ] **Edge Case 3:** [e.g., Run with `--dry-run`] → *Expected behavior: ...*
* [ ] **Edge Case 4:** [e.g., Run with `--json`] → *Expected behavior: valid JSON output*
* [ ] **E2E smoke test:** [Run the actual command end-to-end and verify output]

## 7. Self-Review Checklist

*Before presenting the plan, verify against these anti-patterns.*

- [ ] I have **read every file I plan to modify** — no guessing at APIs or struct signatures.
- [ ] I have **cross-referenced every function call** against the actual source code.
- [ ] I have **not omitted documentation updates** from the plan.
- [ ] My verification plan **tests edge cases**, not just the happy path.
- [ ] I have **not assumed default values** without checking if they can be overridden at runtime.
- [ ] I have audited all **4 agent skill template files** for stale command references.

---

## 8. Open Questions / User Review Required

> [!WARNING]
> [Document anything that requires user review or feedback, such as breaking changes, significant design decisions, or ambiguous requirements.]
