# {{.ProjectName}}

> {{.Description}}

## Quick Start

```bash
devx up                    # Start Postgres + HTTPS tunnel
cp .env.example .env       # Configure DATABASE_URL
npm install
npm run dev                # Start dev server with hot-reload
```

Generated with `devx scaffold node-api`.
