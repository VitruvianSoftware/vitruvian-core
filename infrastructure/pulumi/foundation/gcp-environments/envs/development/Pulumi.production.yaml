# Stack config for the vitruviansoftware.dev foundation Phase 2 — development environment
# (leaf project foundation-environments-development, stack: production).
# The environment identity (development/d) is pinned in this leaf's main.go.
#
# This stack deploys exactly one environment (development):
#   - fldr-development folder under fldr-foundation-1
#   - prj-d-kms (environment KMS project)
#   - prj-d-secrets (environment Secrets project)
#
# Promotion order: development → nonproduction → production
# This is the first environment deployed (auto-approve).
config:
  # --- Core identifiers (mirrors 1-org config) ---
  foundation-environments-development:org_id: "90456063361"
  foundation-environments-development:billing_account: "013EEF-107C95-8E11BD"
  foundation-environments-development:project_prefix: "prj"
  foundation-environments-development:folder_prefix: "fldr"

  # --- Cross-stage references ---
  foundation-environments-development:org_stack_name: "ipv1337/foundation-org-shared/production"
  foundation-environments-development:parent_folder: "823326946563"
