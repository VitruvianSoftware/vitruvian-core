# Stack config for the vitruviansoftware.dev foundation Phase 2 — nonproduction environment
# (leaf project foundation-environments-nonproduction, stack: production).
# The environment identity (nonproduction/n) is pinned in this leaf's main.go.
#
# This stack deploys exactly one environment (nonproduction):
#   - fldr-nonproduction folder under fldr-foundation-1
#   - prj-n-kms (environment KMS project)
#   - prj-n-secrets (environment Secrets project)
#
# Promotion order: development → nonproduction → production
# Requires manual approval via GitHub Environment protection rules.
config:
  # --- Core identifiers (mirrors 1-org config) ---
  foundation-environments-nonproduction:org_id: "90456063361"
  foundation-environments-nonproduction:billing_account: "013EEF-107C95-8E11BD"
  foundation-environments-nonproduction:project_prefix: "prj"
  foundation-environments-nonproduction:folder_prefix: "fldr"

  # --- Cross-stage references ---
  foundation-environments-nonproduction:org_stack_name: "ipv1337/foundation-org-shared/production"
  foundation-environments-nonproduction:parent_folder: "823326946563"
