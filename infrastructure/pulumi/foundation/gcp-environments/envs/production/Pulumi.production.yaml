# Stack config for the vitruviansoftware.dev foundation Phase 2 — production environment
# (leaf project foundation-environments-production, stack: production).
# The environment identity (production/p) is pinned in this leaf's main.go.
#
# This stack deploys exactly one environment (production):
#   - fldr-production folder under fldr-foundation-1
#   - prj-p-kms (environment KMS project)
#   - prj-p-secrets (environment Secrets project)
#
# Promotion order: development → nonproduction → production
# Requires manual approval via GitHub Environment protection rules.
config:
  # --- Core identifiers (mirrors 1-org config) ---
  foundation-environments-production:org_id: "90456063361"
  foundation-environments-production:billing_account: "013EEF-107C95-8E11BD"
  foundation-environments-production:project_prefix: "prj"
  foundation-environments-production:folder_prefix: "fldr"

  # --- Cross-stage references ---
  foundation-environments-production:org_stack_name: "ipv1337/foundation-org-shared/production"
  foundation-environments-production:parent_folder: "823326946563"
