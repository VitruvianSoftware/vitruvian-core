# Stack config for the vitruviansoftware.dev foundation Phase 2 — development environment.
#
# This stack deploys exactly one environment (development):
#   - fldr-development folder under fldr-foundation-1
#   - prj-d-kms (environment KMS project)
#   - prj-d-secrets (environment Secrets project)
#
# Promotion order: development → nonproduction → production
# This is the first environment deployed (auto-approve).
config:
  # --- Environment identity ---
  foundation-environments:env: "development"
  foundation-environments:env_code: "d"

  # --- Core identifiers (mirrors 1-org config) ---
  foundation-environments:org_id: "90456063361"
  foundation-environments:billing_account: "013EEF-107C95-8E11BD"
  foundation-environments:project_prefix: "prj"
  foundation-environments:folder_prefix: "fldr"

  # --- Cross-stage references ---
  foundation-environments:org_stack_name: "ipv1337/foundation-org/production"
  foundation-environments:parent_folder: "823326946563"
