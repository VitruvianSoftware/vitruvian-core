# Copybara bidi-sync pilot test

This file is a temporary marker for verifying the vitruvian-core ↔ mcp-slack
Copybara sync. Safe to delete after the pilot.

sync-marker: import-direction-seed (created on standalone mcp-slack)
export-marker: export-direction-test (added on monorepo vitruvian-core)
iterative-test: import-v2 (standalone change AFTER a peer commit in the range)
iterative-test: export-v3 (monorepo change AFTER a peer import commit in the range)
conflict-line: EDITED-ON-MONOREPO
