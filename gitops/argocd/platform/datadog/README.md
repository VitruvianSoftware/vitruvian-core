# Datadog (stub)

Placeholder mirroring the source gitops; **not enabled**. To activate, add an
`applicationset.yaml` (chart `datadog` from https://helm.datadoghq.com) + values,
and disable Pulumi `DeployDatadog` at cutover.
